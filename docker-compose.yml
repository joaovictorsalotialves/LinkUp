version: "1"

services:
  linkup-pg:
    image: bitnami/postgresql
    environment:
      POSTGRESQL_USERNAME: docker
      POSTGRES_PASSWORD: docker
      POSTGRESQL_DATABASE: linkup
    ports:
      - "5433:5432"
    volumes:
      - ./docker/setup.sql:/docker-entrypoint-initdb.d/setup.sql
